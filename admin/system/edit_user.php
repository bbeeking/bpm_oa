<?php
 define('IN_DAEM', TRUE); include_once '../includes/init.php'; include_once '../includes/system.func.php'; $departmentAry = get_department_kv(); $userid = ceil($_GET['aid']); $sql = "select a_id,a_gid,a_account,a_department,a_username,a_number,a_tel,a_ispermit,a_language from ".DB_DAEMDB.".".TB_SUFFIX."db_adminuser where a_id=".$userid; $result = $db->query_first($sql); if(empty($result)) { gourl('未查询到该用户信息。','',-1); } $result['a_password'] = '********'; $groups = ShowGroup(); foreach ($groups as $v) { $group[$v['g_id']] = $v['g_description']; } include template('system','add_user'); 